import { IndexKind } from "typescript"

export class Topic {
    constructor(title, description, image) {
        this.title = title
        this.description = description
        this.image = image
    }
}